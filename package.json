{
  "name": "duck-typescript",
  "version": "1.0.0",
  "description": "duck typescript evilbinary.org",
  "main": "ts-node duck.ts",
  "scripts": {
    "install": "npm -v",
    "build": "node-gyp -j 8 configure build",
    "main": "ts-node -r tsconfig-paths/register -p main.ts",
    "start": "ts-node -r tsconfig-paths/register",
    "testall": "mocha -r ts-node/register tests/**/tests.ts"
  },
  "keywords": [
    "duck-typescript",
    "duck",
    "typescript"
  ],
  "author": "evilbinary",
  "license": "ISC",
  "dependencies": {
    "@types/ffi": "^0.2.2",
    "ffi": "^2.3.0"
  },
  "devDependencies": {
    "tsconfig-paths": "^3.8.0"
  }
}
